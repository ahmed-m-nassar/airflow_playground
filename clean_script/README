# Orders ETL Pipeline (with URL Data Source)

This project extracts, transforms, and loads data from a CSV hosted online.

## Data Source
We use the [Tips dataset](https://raw.githubusercontent.com/mwaskom/seaborn-data/master/tips.csv).

## Steps
1. **Extract** raw data from URL
2. **Transform**:
   - Remove null values
   - Keep only groups with more than 2 people
   - Calculate `total_bill_per_person`
3. **Load** cleaned data into a new CSV

## Setup

```bash
python -m venv venv
source venv/bin/activate   # mac/linux
venv\Scripts\activate      # windows
pip install -r requirements.txt
